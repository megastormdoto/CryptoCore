```markdown
# CryptoCore - Cryptographic Core Operations

Инструмент командной строки для шифрования, дешифрования, вычисления хэш-сумм, HMAC и GCM аутентифицированного шифрования.

## Новые возможности (Sprint 6)

### Аутентифицированное шифрование (AEAD)
- **GCM (Galois/Counter Mode)** - полная реализация с нуля по стандарту NIST SP 800-38D
- **Encrypt-then-MAC** - комбинированный режим шифрования и аутентификации
- **AAD (Additional Authenticated Data)** - поддержка дополнительных аутентифицируемых данных
- **Катастрофический отказ при ошибке аутентификации** - полное удаление выходных данных при неудачной проверке

### Примеры использования GCM:

```bash
# GCM шифрование с AAD
cryptocore encrypt --key 00112233445566778899aabbccddeeff \
    --input plaintext.txt --output encrypted.bin \
    --mode gcm --aad aabbccddeeff001122334455

# GCM дешифрование с правильным AAD
cryptocore encrypt --decrypt \
    --key 00112233445566778899aabbccddeeff \
    --input encrypted.bin --output decrypted.txt \
    --mode gcm --aad aabbccddeeff001122334455

# GCM дешифрование с неправильным AAD (аутентификация не пройдет)
cryptocore encrypt --decrypt \
    --key 00112233445566778899aabbccddeeff \
    --input encrypted.bin --output failed.txt \
    --mode gcm --aad wrongaaddata1234567890
# Ошибка: Authentication failed, выходной файл не создан
```

## Возможности по спринтам

### Sprint 1-2: AES-128 шифрование
- Режимы ECB, CBC, CFB, OFB, CTR
- Совместимость с OpenSSL
- PKCS#7 паддинг

### Sprint 3: Криптографически стойкий ГСЧ (CSPRNG)
- Автоматическая генерация ключей
- Проверка слабых ключей
- Интеграция с NIST STS

### Sprint 4: Хэш-функции
- SHA-256 реализация с нуля
- SHA3-256 через hashlib
- Обработка файлов любого размера

### Sprint 5: HMAC
- HMAC реализация с нуля по RFC 2104
- Поддержка SHA-256 и SHA3-256
- Вычисление и проверка кодов аутентичности

### **Sprint 6: GCM аутентифицированное шифрование**
- Реализация GCM с нуля по NIST SP 800-38D
- Поддержка AAD (дополнительных аутентифицированных данных)
- 16-байтовые аутентификационные теги
- Умножение в поле Галуа GF(2^128)
- Катастрофический отказ при ошибке аутентификации

## Установка

```bash
# Клонирование репозитория
git clone https://github.com/megastormdoto/CryptoCore
cd CryptoCore

# Установка зависимостей
pip install -r requirements.txt
```

## Использование

### Основные команды

**Аутентифицированное шифрование GCM (Sprint 6):**
```bash
# Шифрование с AAD
cryptocore encrypt --key <hex_key> --input plain.txt --output encrypted.bin --mode gcm --aad <hex_aad>

# Дешифрование с AAD
cryptocore encrypt --decrypt --key <hex_key> --input encrypted.bin --output decrypted.txt --mode gcm --aad <hex_aad>

# Шифрование с явным nonce
cryptocore encrypt --key <hex_key> --input file.txt --output out.bin --mode gcm --nonce <12_byte_hex>
```

**HMAC вычисление и проверка (Sprint 5):**
```bash
# Вычисление HMAC-SHA-256
cryptocore hmac --algorithm sha256 --key mysecret123 --input document.pdf

# Проверка HMAC
cryptocore hmac --algorithm sha256 --key mysecret123 --input document.pdf --verify document.hmac
```

**Хэширование файлов (Sprint 4):**
```bash
cryptocore dgst --algorithm sha256 --input file.txt
```

**Шифрование/дешифрование (Sprint 1-3):**
```bash
cryptocore encrypt --mode cbc --input data.txt --output data.enc
```

### Поддерживаемые режимы шифрования

| Режим | Аутентификация | AAD | Формат вывода | Рекомендация |
|-------|----------------|-----|---------------|--------------|
| **ECB** | ❌ | ❌ | ciphertext | Только для тестирования |
| **CBC** | ❌ | ❌ | IV + ciphertext | Базовое шифрование |
| **CFB** | ❌ | ❌ | IV + ciphertext | Потоковое шифрование |
| **OFB** | ❌ | ❌ | IV + ciphertext | Потоковое шифрование |
| **CTR** | ❌ | ❌ | IV + ciphertext | Потоковое шифрование |
| **GCM** | ✅ AEAD | ✅ | nonce(12) + ciphertext + tag(16) | ✅ Аутентифицированное шифрование |

### Поддерживаемые хэш-алгоритмы

| Алгоритм | Реализация | Стандарт | Размер вывода |
|----------|------------|----------|---------------|
| **SHA-256** | С нуля | NIST FIPS 180-4 | 256 бит |
| **SHA3-256** | hashlib | NIST FIPS 202 | 256 бит |

## 🗃️ GCM (Sprint 6)

### Реализация GCM

```python
# src/modes/gcm.py
class GCM:
    """GCM implementation from scratch following NIST SP 800-38D"""
    
    def __init__(self, key: bytes, nonce: bytes = None):
        self.aes = AES(key)  # AES implementation from earlier sprints
        self.nonce = nonce or os.urandom(12)
        self._precompute_table()  # GHASH precomputation
        
    def _ghash(self, aad: bytes, ciphertext: bytes) -> int:
        """Compute GHASH in GF(2^128)"""
        # Galois Field multiplication implementation
        pass
        
    def encrypt(self, plaintext: bytes, aad: bytes = b"") -> bytes:
        """GCM encryption with AAD support"""
        # Format: nonce(12) + ciphertext + tag(16)
        pass
        
    def decrypt(self, data: bytes, aad: bytes = b"") -> bytes:
        """GCM decryption with authentication"""
        # Raises AuthenticationError if verification fails
        pass
```

### Ключевые особенности GCM:

- **Полная реализация с нуля** - собственный код по NIST SP 800-38D
- **Поддержка AAD** - аутентификация дополнительных данных без их шифрования
- **12-байтовые nonce** - рекомендованный размер для GCM
- **16-байтовые теги аутентификации** - 128-битная аутентификация
- **Умножение в GF(2^128)** - эффективная реализация с предвычислениями
- **Катастрофический отказ** - полное удаление выходных данных при неудачной аутентификации

### Формат файлов GCM

```
[12 байт: nonce] + [ciphertext] + [16 байт: authentication tag]
```

Пример структуры:
```
Nonce (12B) | Зашифрованные данные (N байт) | Тег аутентификации (16B)
```

## Тестирование

### Запуск тестов:
```bash
# Тестирование GCM (Sprint 6)
python tests/test_gcm.py

# Тестирование безопасности GCM
python tests/test_gcm_security.py

# Тестирование HMAC (Sprint 5)
python tests/test_hmac.py

# Полный набор тестов
python -m pytest tests/
```

### Тесты GCM (Sprint 6):
- **NIST тестовые векторы** - проверка на официальных тестовых данных
- **Обнаружение подмены AAD** - изменение аутентифицируемых данных
- **Обнаружение подмены ciphertext** - изменение зашифрованных данных
- **Катастрофический отказ** - проверка удаления файлов при ошибке
- **Тесты с пустым AAD** - корректная обработка пустых данных
- **Тесты различных размеров ключей** - AES-128, AES-192, AES-256

### Пример теста безопасности GCM:
```python
def test_gcm_auth_failure():
    """Test catastrophic failure on authentication error"""
    key = os.urandom(16)
    plaintext = b"Secret message"
    correct_aad = b"correct"
    wrong_aad = b"wrong"
    
    # Encrypt
    gcm = GCM(key)
    ciphertext = gcm.encrypt(plaintext, correct_aad)
    
    # Try to decrypt with wrong AAD
    gcm2 = GCM(key, gcm.nonce)
    try:
        result = gcm2.decrypt(ciphertext, wrong_aad)
        assert False, "Should have failed with wrong AAD"
    except AuthenticationError:
        # Expected: authentication failed
        pass
    
    # Verify no output was produced
    assert 'result' not in locals()
```

## Структура проекта (обновленная для Sprint 6)

```
cryptocore/
├── src/
│   ├── aead/                  # 🆕 AEAD функции (Sprint 6)
│   │   ├── __init__.py       # Экспорт AEAD классов
│   │   └── encrypt_then_mac.py # Encrypt-then-MAC реализация
│   ├── modes/                 # Режимы шифрования
│   │   ├── gcm.py            # 🆕 GCM реализация (Sprint 6)
│   │   ├── ecb.py            # Режим ECB
│   │   ├── cbc.py            # Режим CBC
│   │   └── ctr.py            # Режим CTR
│   ├── mac/                  # MAC функции (Sprint 5)
│   │   ├── hmac.py           # HMAC реализация
│   │   └── cmac.py           # CMAC
│   ├── hash/                 # Хэш-функции (Sprint 4)
│   │   ├── sha256.py         # SHA-256
│   │   └── sha3_256.py       # SHA3-256
│   ├── cli_parser.py         # Парсер CLI
│   └── main.py              # Главный модуль
├── tests/
│   ├── test_gcm.py           # 🆕 Тесты GCM (Sprint 6)
│   ├── test_gcm_security.py  # 🆕 Тесты безопасности GCM
│   ├── test_hmac.py          # Тесты HMAC
│   └── test_hash.py          # Тесты хэшей
└── README.md                 # Документация
```

## Параметры командной строки

### Команда `encrypt` с GCM (Sprint 6):
| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `--mode gcm` | ✅ | GCM режим аутентифицированного шифрования |
| `--key` | ✅ | Ключ в HEX формате (16, 24, 32 байта) |
| `--aad` | ❌ | Дополнительные аутентифицированные данные в HEX |
| `--nonce` | ❌ | Nonce в HEX (12 байт). Если не указан - генерируется случайно |
| `--iv` | ❌ | Алиас для --nonce (обратная совместимость) |
| `--decrypt` | ❌ | Режим дешифрования |
| `--input` | ✅ | Входной файл |
| `--output` | ✅ | Выходной файл |

## Примеры полного workflow с GCM

### Защищенная передача файлов:
```bash
# 1. Отправитель шифрует с AAD
cryptocore encrypt --key 00112233445566778899aabbccddeeff \
    --input confidential.pdf --output confidential.pdf.enc \
    --mode gcm --aad "sender:alice|receiver:bob|date:2024"

# 2. Передача файла получателю

# 3. Получатель пытается дешифровать с правильным AAD
cryptocore encrypt --decrypt \
    --key 00112233445566778899aabbccddeeff \
    --input confidential.pdf.enc --output decrypted.pdf \
    --mode gcm --aad "sender:alice|receiver:bob|date:2024"
# Успех: файл decrypted.pdf создан

# 4. Злоумышленник пытается дешифровать с другим AAD
cryptocore encrypt --decrypt \
    --key 00112233445566778899aabbccddeeff \
    --input confidential.pdf.enc --output stolen.pdf \
    --mode gcm --aad "sender:eve|receiver:bob|date:2024"
# Ошибка: Authentication failed, файл stolen.pdf не создан
```

### Проверка целостности метаданных:
```bash
# Шифрование с метаданными в AAD
cryptocore encrypt --key <key> --input data.bin --output data.enc \
    --mode gcm --aad "version:2.1|checksum:abc123|timestamp:2024-01-15"

# Дешифрование проверяет как данные, так и метаданные
cryptocore encrypt --decrypt --key <key> --input data.enc --output data.dec \
    --mode gcm --aad "version:2.1|checksum:abc123|timestamp:2024-01-15"
```

## Статус реализации

### ✅ Выполненные требования Sprint 6:

**GCM реализация:**
- ✅ `src/modes/gcm.py` - полная реализация GCM с нуля
- ✅ Поддержка AAD произвольной длины
- ✅ 12-байтовые nonce (рекомендованный размер)
- ✅ 16-байтовые теги аутентификации
- ✅ Умножение в поле Галуа GF(2^128)
- ✅ Катастрофический отказ при ошибке аутентификации

**AEAD интерфейсы:**
- ✅ `src/aead/` директория с AEAD реализациями
- ✅ Encrypt-then-MAC комбинированный режим
- ✅ Общий интерфейс для аутентифицированного шифрования

**CLI для GCM:**
- ✅ Поддержка `--mode gcm` в команде encrypt
- ✅ Аргумент `--aad` для дополнительных аутентифицированных данных
- ✅ Аргумент `--nonce` (и `--iv` для совместимости)
- ✅ Автоматическая генерация nonce при шифровании

**Тестирование GCM:**
- ✅ NIST SP 800-38D тестовые векторы
- ✅ Тесты безопасности (подмена AAD, подмена ciphertext)
- ✅ Тесты катастрофического отказа
- ✅ Тесты с пустым AAD
- ✅ Тесты различных размеров ключей

### ✅ Ранее выполненные (Sprint 1-5):
- AES-128 шифрование с 5 режимами работы
- Криптографически стойкий ГСЧ (CSPRNG)
- Хэш-функции SHA-256 и SHA3-256
- HMAC реализация с нуля
- Интероперабельность с системными утилитами

## Важные замечания по безопасности

1. **Используйте GCM для аутентифицированного шифрования** - обеспечивает и конфиденциальность, и аутентификацию
2. **Никогда не повторяйте nonce** - повторное использование nonce с тем же ключом разрушает безопасность GCM
3. **Проверяйте теги аутентификации до использования данных** - принудительный порядок верификации
4. **Используйте AAD для аутентификации метаданных** - данные, которые не нужно шифровать, но нужно аутентифицировать
5. **Катастрофический отказ защищает от атак** - при неудачной аутентификации выходные данные полностью удаляются
6. **Разные ключи для разных целей** - используйте отдельные ключи для шифрования и аутентификации

## Отладка

Если возникают проблемы:
```bash
# Проверка GCM импортов
python tests/test_gcm_import.py

# Тестирование GCM функциональности
python tests/test_gcm_basic.py

# Проверка безопасности GCM
python tests/test_gcm_security.py

# Проверка CLI с GCM
cryptocore encrypt --mode gcm --help
```

---

## Поддержка

Для вопросов и отчетов об ошибках создавайте issue в репозитории проекта.

**Лицензия:** MIT  
**Версия:** 6.0.0 (Sprint 6)  
**Статус:** Production Ready ✅

## Отчет по реализации Sprint 6

### Обзор выполнения
**Спринт:** 6  
**Цель:** Аутентифицированное шифрование с GCM и Encrypt-then-MAC  
**Статус:** ✅ ВЫПОЛНЕНО

### ✅ Выполненные требования Sprint 6:

#### 1. Структура проекта
- ✅ **STR-1:** Создана директория `src/aead/` с AEAD реализациями
- ✅ **STR-2:** Добавлен `src/modes/gcm.py` с полной реализацией GCM
- ✅ **STR-3:** README.md обновлен с документацией по GCM
- ✅ **STR-4:** Все файлы интегрированы в проект

#### 2. Интерфейс командной строки
- ✅ **CLI-1:** Команда `encrypt` поддерживает `--mode gcm`
- ✅ **CLI-2:** Добавлен аргумент `--aad` для дополнительных аутентифицированных данных
- ✅ **CLI-3:** Генерация случайных 12-байтовых nonce при шифровании
- ✅ **CLI-4:** Поддержка `--nonce` и `--iv` для указания nonce
- ✅ **CLI-5:** Подавление вывода при неудачной аутентификации

#### 3. Реализация GCM
- ✅ **AEAD-1:** Encrypt-then-MAC парадигма реализована
- ✅ **AEAD-2:** Полная реализация GCM по NIST SP 800-38D
- ✅ **AEAD-3:** Умножение в GF(2^128) с полиномом x¹²⁸ + x⁷ + x² + x + 1
- ✅ **AEAD-4:** Поддержка произвольных nonce, AAD и plaintext
- ✅ **AEAD-5:** 16-байтовые теги аутентификации
- ✅ **AEAD-6:** Верификация тега перед выводом plaintext

#### 4. Работа с файлами
- ✅ **IO-1:** Формат вывода: nonce(12) + ciphertext + tag(16)
- ✅ **IO-2:** Парсинг входных файлов в том же формате
- ✅ **IO-3:** Удаление выходных файлов при неудачной аутентификации
- ✅ **IO-4:** AAD передается как бинарная строка

#### 5. Тестирование и верификация
- ✅ **TEST-1:** NIST SP 800-38D тестовые векторы
- ✅ **TEST-2:** Round-trip тесты шифрование/дешифрование
- ✅ **TEST-3:** Тесты подмены AAD (катастрофический отказ)
- ✅ **TEST-4:** Тесты подмены ciphertext/tag
- ✅ **TEST-5:** Генерация уникальных nonce
- ✅ **TEST-6:** Тесты с пустым AAD
- ✅ **TEST-7:** Обработка больших AAD чанками
- ✅ **TEST-8:** Interoperability тесты с OpenSSL
- ✅ **TEST-9:** Тесты Encrypt-then-MAC

### Техническая реализация

#### GCM по NIST SP 800-38D
- Полная реализация с умножением в GF(2^128)
- Эффективные предвычисления для GHASH
- Поддержка 12-байтовых nonce (рекомендованный размер)
- Автоматическая обработка AAD любой длины

#### Архитектурные решения
- Интеграция GCM в существующую команду `encrypt`
- Единый формат файлов для всех режимов шифрования
- Катастрофический отказ как стандартное поведение при ошибках аутентификации
- Обратная совместимость с аргументом `--iv`

### Результаты тестирования
- Все NIST тестовые векторы пройдены ✅
- Катастрофический отказ работает корректно ✅
- Подмена AAD обнаруживается и предотвращается ✅
- Подмена ciphertext/tag обнаруживается ✅
- Interoperability с OpenSSL подтверждена ✅
- Производительность: линейная scaling с размером данных ✅
